{% extends 'accueil.html.twig' %}

{% block title %}Gestion des Entrees{% endblock %}
{% block titleh1 %}Gestion des Entrees{% endblock %}
{% block Item %}Entrees{% endblock %}

{% block body %}
<div class="row">
    <div class="container col-md-6">
        <div class="card">
            <div class="card-header text-center bg-primary text-white">Liste des Achats</div>
            <div class="card-body">
                <table class="table table-striped table-bordered">
                    <thead class="thead-dark text-center text-white">
                    <tr>
                        <th scope="col">Identifiant</th>
                        <th scope="col">date</th>
                        <th scope="col">Quantite acheté</th>
                        <th scope="col">Prix</th>
                        <th scope="col">Produit</th>

                    </tr>
                    </thead>
                    {% for e in entrees %}
                    <tr>
                        <th scope="row">{{ e.id }}</th>
                       <td>{{ e.date| date('d/m/y') }}</td>
                        <td>{{ e.qte }}</td>
                         <td>{{ e.prix }}</td>
                         <td>{{ e.produit }}</td>
                    </tr>
                    {% endfor %}
                </table>
            </div>
            <div class="card-footer">LICENCEGENIELOGICIEL</div>
        </div>
    </div>
    <div class="container col-md-6">    
        <div class="card">
            <div class="card-header text-center bg-primary text-white">Formulaire de Gestion des Achats</div>
            <div class="card-body">
                {{ form(form) }}
            </div>
            <div class="card-footer">LICENCEGENIELOGICIEL</div>
        </div>
    </div>
</div>
{% endblock %}
                